const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case "sdfjklsdj":
      return state;

    default:
      return state;
  }
};
export default reducer;
